import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'issue-ion-nav-spec',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
